
const User = window['User'] || {
    redirect: function(url, param) {
      var form = document.createElementByName("form");
      form.method = "GET";
      form.enctype = "";
      form.action = "";

    }

};
